
#include "definitions.h"

/* ------------------------------------------------------------------------------------------------
 * Returns a vector to AuthorData for the repo given as the parameter: repo
 * The vector represents a set of all authors for the given repo, and the struct contains all relevant data we need
 * 
 * Returns an empty vector on failure
 * ------------------------------------------------------------------------------------------------ */
std::vector<AuthorData> getAllAuthorData(git_repository* repo) 
{
  return {};
}
