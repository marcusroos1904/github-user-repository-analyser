
#include "definitions.h"

/* ------------------------------------------------------------------------------------------------
 * Calculates and return the URE score based on the MRE score that was passed as the parameter: MRE_score
 * and the data that was passed as the parameter: all_authors
 * 
 * Returns the URE score on success and -1 on failure
 * ------------------------------------------------------------------------------------------------ */
float calculateURE(int MRE_score, const std::vector<AuthorData>& all_authors) 
{
  return -1.0f;
}
